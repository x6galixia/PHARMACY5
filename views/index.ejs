<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/skeleton/2.0.4/skeleton.css"
    />
    <title>Home</title>
  </head>
  <body>
<!------------------------HEADER----------------------------------->
    <h1>Welcome, <%= userType %> <%= username %>!</h1>

    <form action="/logout?_method=DELETE" method="POST">
        <div> 
          <button type="submit">Log out</button>
        </div>
    </form>
<!------------------------SEARCH--------------------------------------------->
    <form action="/search" method="post">
      <input type="text" name="search" placeholder="search">
      <button type="submit">search</button>
    </form>
<!---------------------------ADD STOCKS---------------------------------------------->
<button onclick="window.location.href='addstocks'">Add Stocks</button>

<!---------------------------ADD REQUESTS---------------------------------------------->
  <button onclick="window.location.href='addrequest'">Add Request</button>

<!---------------------------DISPENSE---------------------------------------------->
<button onclick="window.location.href='dispense'">Dispense</button>
<!---------------------------RECORDS---------------------------------------------->
<button onclick="window.location.href='records'">Records</button>

    <!---------------TABLE--------------------------------------------------->
    <table>
      <thead>
        <tr>
          <th>Item Name</th>
          <th>Brand Name</th>
          <th>Manufacturer</th>
          <th>Dosage</th>
          <th>Expiration</th>
          <th>Quantity</th>
        </tr>
      </thead>
      <tbody>
        <% if (viewList && viewList.length > 0) { %> <% viewList.forEach(item =>
        { %>
        <tr>
          <td><%= item.itemName %></td>
          <td><%= item.brand %></td>
          <td><%= item.manufacturer %></td>
          <td><%= item.dosage %></td>
          <td><%= item.expiration %></td>
          <td><%= item.quantity %></td>
        </tr>
        <% }) %> <% } else { %>
        <tr>
          <td colspan="6">No items found.</td>
        </tr>
        <% } %>
      </tbody>
    </table>

  </body>
</html>